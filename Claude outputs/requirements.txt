# ===========================================================
# KAMP_LJP - 전자부품(배터리팩) 예지보전 AI 데이터셋 연습 프로젝트
#
# 가상환경 생성 (Python 내장 venv 기준)
#   python -m venv .venv
#   .venv\Scripts\Activate.ps1      (Windows PowerShell)
#   python -m pip install --upgrade pip
#   pip install -r requirements.txt
#   python -m ipykernel install --user --name KAMP --display-name "KAMP"
#
# 대부분 "최소 버전(>=)"만 지정했습니다. 특정 Python 버전에서만 빌드되는 구버전
# (가이드북 원본: pandas==1.5.2, numpy==1.23.5 등)을 그대로 고정하면 최신 Python에서
# 미리 빌드된 wheel이 없어 빌드 실패가 날 수 있어 지양했습니다. 최소 버전만 지정하면 pip가
# 현재 설치된 Python과 호환되는 최신 안정 버전을 알아서 골라 설치하므로, 새 패키지를 추가로
# 설치할 때도 충돌 가능성이 낮아집니다.
#
# 작업이 어느 정도 안정화된 뒤에는 `pip freeze > requirements-lock.txt` 로 그 시점의
# 정확한 버전을 별도 고정해두면 재현성을 확보할 수 있습니다.
# ===========================================================

# ---- 데이터 처리 / EDA ----
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
plotly>=5.14
seaborn>=0.12
scikit-learn>=1.3

# ---- 노트북 실행 환경 ----
jupyter>=1.0
ipykernel>=6.25
nbformat>=5.9

# ---- AI 모델 학습 (N-HiTS, 03_nhits_model.ipynb) ----
# 아래 조합은 위 EDA 패키지들(pandas>=2.0, numpy>=1.24)과 같은 가상환경에
# 실제로 설치·학습·평가까지 완료되어 충돌 없음을 확인했습니다 (2026-09-10 검증).
#
# 설치 명령 (Windows PowerShell, 이미 만든 .venv 안에서):
#   pip install torch pytorch-lightning pytorch-forecasting
#
# 주의: --index-url https://download.pytorch.org/whl/... 를 쓰지 말고 기본 PyPI로
# 설치하세요. 사내/기관 네트워크에 따라 download.pytorch.org가 막혀 있는 경우가 있고,
# 기본 PyPI(torch>=2.x, CPU 환경에서도 동작)로도 문제없이 설치·학습됩니다.
#
# pytorch_forecasting >= 1.0 부터 NHiTS가 `lightning.pytorch.LightningModule`을
# 상속하므로, 노트북에서도 `import pytorch_lightning as pl`이 아니라
# `import lightning.pytorch as pl` / `from lightning.pytorch.callbacks import EarlyStopping`
# 로 import 해야 합니다 (03_nhits_model.ipynb에 반영되어 있음).
torch>=2.0
pytorch-lightning>=2.1
pytorch-forecasting>=1.0

# ===========================================================
# 참고: 가이드북 원본은 torch==1.12.1 / pytorch_lightning==1.7.7 /
# pytorch_forecasting==0.10.3 처럼 2022년 버전으로 고정되어 있으나, 이 구버전 조합은
# numpy<1.24, pandas<2.0을 요구해 위 EDA 패키지들과 충돌합니다. 가이드북과 똑같은 옛
# 버전으로 재현하고 싶다면 아래처럼 별도의 독립된 가상환경(.venv-model 등)을 만드세요.
#
#      torch==1.12.1+cu116 torchvision==0.13.1+cu116   # GPU: --extra-index-url https://download.pytorch.org/whl/cu116
#      pytorch_lightning==1.7.7
#      pytorch_forecasting==0.10.3
#      pytorch-forecasting[mqf2]==0.10.3
#      cpflows==0.1.2
#      pandas==1.5.2
#      numpy==1.23.5
# ===========================================================
